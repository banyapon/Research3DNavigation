using System.Numerics;
using UnityEngine;
using UnityEngine.Splines;


public class GetDistanceBetweenTs : MonoBehaviour
{
    public SplineContainer splineA, splineB;
    public float totalDistance;

    // Start is called before the first frame update
    void Start()
    {
        //float lengthToEnd = SplineUtility.CalculateLength(splineA, 0, t);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
